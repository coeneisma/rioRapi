% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine_data.R
\name{rio_visualize_dataset_connections}
\alias{rio_visualize_dataset_connections}
\title{Generate a visualization of dataset connections}
\usage{
rio_visualize_dataset_connections(
  ...,
  structure = NULL,
  min_confidence = "medium",
  interactive = TRUE,
  highlight_datasets = NULL
)

rio_visualize_dataset_connections(
  ...,
  structure = NULL,
  min_confidence = "medium",
  interactive = TRUE,
  highlight_datasets = NULL
)
}
\arguments{
\item{...}{Optional named tibbles to visualize connections between.}

\item{structure}{Optional RIO structure mapping as returned by rio_load_structure().
If NULL and no tibbles are provided, it will be loaded automatically.}

\item{min_confidence}{Minimum confidence level for connections.
Can be "high" or "medium". Default is "medium".}

\item{interactive}{Logical indicating whether to create an interactive visualization
(requires visNetwork package). Default is TRUE.}

\item{highlight_datasets}{Character vector of dataset names to highlight in the visualization.}
}
\value{
A visualization object showing the connections between datasets.

A visualization object showing the connections between datasets.
}
\description{
This function creates a visualization showing the connections between RIO datasets,
either from a structure mapping or from the datasets provided.

This function creates a visualization showing the connections between RIO datasets,
either from a structure mapping or from the datasets provided.
}
\examples{
\dontrun{
# Visualize connections from the structure mapping
rio_visualize_dataset_connections()

# Visualize connections between specific datasets
locations <- rio_get_locations(city = "Amsterdam")
institutions <- rio_get_data(dataset_name = "onderwijsinstellingserkenningen")
vestigingen <- rio_get_data(dataset_name = "vestigingserkenningen")

rio_visualize_dataset_connections(
  locations = locations,
  institutions = institutions,
  vestigingen = vestigingen,
  highlight_datasets = c("locations", "vestigingen")
)
}

\dontrun{
# Visualize connections from the structure mapping
rio_visualize_dataset_connections()

# Visualize connections between specific datasets
locations <- rio_get_locations(city = "Amsterdam")
institutions <- rio_get_data(dataset_name = "onderwijsinstellingserkenningen")
vestigingen <- rio_get_data(dataset_name = "vestigingserkenningen")

rio_visualize_dataset_connections(
  locations = locations,
  institutions = institutions,
  vestigingen = vestigingen,
  highlight_datasets = c("locations", "vestigingen")
)
}

}
